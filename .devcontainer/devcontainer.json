{
  "image": "mcr.microsoft.com/vscode/devcontainers/miniconda",
  "features": {
  },
  "waitFor": "onCreateCommand",
  "updateContentCommand": "conda install -y numpy scipy pandas matplotlib jupyter nb_conda tqdm seaborn && conda install -y pytorch torchvision torchaudio cpuonly -c pytorch  && conda install -y -c conda-forge obspy cartopy",
  // "updateContentCommand": "conda env update -n base -f environment.yml",
  "postCreateCommand": "",
  "customizations": {
    "codespaces": {
      "openFiles": []
    },
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-python.python"
      ]
    }
  }
}
